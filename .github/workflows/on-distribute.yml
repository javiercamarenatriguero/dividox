name: On Distribute

on:
  workflow_dispatch:
    inputs:
      branch:
        description: Branch to build and distribute
        required: true
        default: main
      release_notes:
        description: Release notes (optional)
        required: false
        default: ""

jobs:
  prepare:
    name: Compute Build Number
    runs-on: ubuntu-latest
    outputs:
      build_number: ${{ steps.build_number.outputs.value }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Compute build number
        id: build_number
        run: echo "value=$(git rev-list --count HEAD)" >> $GITHUB_OUTPUT

  build:
    name: Build Android
    runs-on: ubuntu-latest
    needs: [prepare]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Android
        run: ./gradlew :composeApp:assembleDebug -PbuildNumber=${{ needs.prepare.outputs.build_number }}

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: composeApp/build/outputs/apk/debug/*.apk
          retention-days: 1

  distribute-android:
    name: Distribute Android
    needs: [prepare, build]
    uses: ./.github/workflows/distribute-android-action.yml
    with:
      build_number: ${{ needs.prepare.outputs.build_number }}
      commit_sha: ${{ github.sha }}
      release_notes: ${{ inputs.release_notes != '' && inputs.release_notes || format('[{0}] Build {1}', inputs.branch, needs.prepare.outputs.build_number) }}
    secrets:
      FIREBASE_APP_ID_ANDROID: ${{ secrets.FIREBASE_APP_ID_ANDROID }}
      FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

  distribute-desktop:
    name: Distribute Desktop
    needs: [prepare, build]
    uses: ./.github/workflows/distribute-desktop-action.yml
    with:
      build_number: ${{ needs.prepare.outputs.build_number }}
      tag: "${{ inputs.branch }}-build-${{ needs.prepare.outputs.build_number }}"
      release_notes: ${{ inputs.release_notes != '' && inputs.release_notes || format('[{0}] Build {1}', inputs.branch, needs.prepare.outputs.build_number) }}
    permissions:
      contents: write
